import pandas as pd
from sklearn.model_selection import train_test_split

class utils():
    
    def loadData(self,imagesAddr,manAddr):
        man = pd.read_csv(manAddr)
        man.apply(lambda add: './' )
        pass

    def Split(slef):
        pass
        
    def visualizeData(self):
        pass